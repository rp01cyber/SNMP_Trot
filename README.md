# SNMP_Trot
This is a bash script used to run snmp recursively on a targets file of IP addresses and then export all information to a text file. This tool specifically only logs the string data for review so you don't pull the MIB tree aspects.
